{
  "$schema": "../anatomy.schema.json",
  "name": "tooltip",
  "behavior": "APG tooltip pattern (shows on trigger hover/focus after delay, hides on pointer leave/blur; Esc dismisses without moving focus; role=tooltip referenced by the trigger's aria-describedby; never contains interactive content)",
  "parts": [
    { "part": "root", "tokens": ["inverse-surface", "on-inverse-surface", "radius-control", "elevation-overlay", "layer-tooltip", "space-inline-sm"], "note": "surface choice follows the theme's elevation-strategy: inverse-surface/on-inverse-surface for inverted style (shadcn default), OR surface-overlay/on-surface-overlay + border where the theme expresses depth by border (Salt). A recipe difference — anatomy unchanged." },
    { "part": "arrow", "optional": true, "tokens": ["inverse-surface"], "note": "fill always matches the root surface recipe" },
    { "part": "label", "contentSlot": true, "tokens": ["type-caption", "on-inverse-surface"] }
  ],
  "variantAxes": {},
  "states": ["rest", "open"],
  "motion": { "enter": { "duration": "duration-fast", "easing": "easing-enter" }, "exit": { "duration": "duration-instant", "easing": "easing-exit" } }
}
